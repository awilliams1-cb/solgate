// Auto-generated by solgate from challenged_proposal.gate
// DO NOT EDIT — re-run `solgate codegen` to regenerate
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import {Vm} from "forge-std/Vm.sol";
import {Solgate} from "solgate/Solgate.sol";

library ChallengedProposalMocks {
    using Solgate for Solgate.Gate;

    Vm constant VM = Vm(address(uint160(uint256(keccak256("hevm cheat code")))));

    // ─── Params ───

    function setDisputeGame(Solgate.Gate memory gate, address value) internal {
        gate.setParam("disputeGame", value);
    }

    function setHonestProposer(Solgate.Gate memory gate, address value) internal {
        gate.setParam("honestProposer", value);
    }

    function setHonestChallenger(Solgate.Gate memory gate, address value) internal {
        gate.setParam("honestChallenger", value);
    }

    // ─── Event Sources ───

    // moveEvents: list<tuple<integer, bytes, address>>
    // From: event Move(uint256 indexed parentIndex, bytes32 indexed claim, address indexed claimant)
    bytes32 constant MOVE_EVENTS_SELECTOR = keccak256("Move(uint256,bytes32,address)");

    struct MoveEventsEntry {
        int256 parentIndex;
        bytes32 claim;
        address claimant;
    }

    function mockMoveEvents(Solgate.Gate memory gate, MoveEventsEntry[] memory entries) internal {
        string memory json = "[";
        for (uint256 i = 0; i < entries.length; i++) {
            if (i > 0) json = string.concat(json, ",");
            json = string.concat(json, "[",
                VM.toString(entries[i].parentIndex), ",",
                '"', VM.toString(entries[i].claim), '"', ",",
                '"', VM.toString(entries[i].claimant), '"',
            "]");
        }
        json = string.concat(json, "]");
        gate.mockRaw("moveEvents", json);
    }

    // ─── Call Sources ───

    // claimCount: integer
    // From: function claimDataLen() view returns (uint256)
    function mockClaimCount(Solgate.Gate memory gate, int256 value) internal {
        gate.mockInt("claimCount", value);
    }

    // claimData: list<tuple<integer, address, address, integer, bytes, integer, integer>>
    // From: function claimData(uint256 idx) view returns (uint32,address,address,uint128,bytes32,uint128,uint128)
    struct ClaimDataEntry {
        int256 field0;
        address field1;
        address field2;
        int256 field3;
        bytes32 field4;
        int256 field5;
        int256 field6;
    }

    function mockClaimData(Solgate.Gate memory gate, ClaimDataEntry[] memory entries) internal {
        string memory json = "[";
        for (uint256 i = 0; i < entries.length; i++) {
            if (i > 0) json = string.concat(json, ",");
            json = string.concat(json, "[",
                VM.toString(entries[i].field0), ",",
                '"', VM.toString(entries[i].field1), '"', ",",
                '"', VM.toString(entries[i].field2), '"', ",",
                VM.toString(entries[i].field3), ",",
                '"', VM.toString(entries[i].field4), '"', ",",
                VM.toString(entries[i].field5), ",",
                VM.toString(entries[i].field6),
            "]");
        }
        json = string.concat(json, "]");
        gate.mockRaw("claimData", json);
    }

    // rootClaimProposer: address
    function mockRootClaimProposer(Solgate.Gate memory gate, address value) internal {
        gate.mockAddress("rootClaimProposer", value);
    }

    // attackClaimParentIndices: list<integer>
    function mockAttackClaimParentIndices(Solgate.Gate memory gate, int256[] memory values) internal {
        string memory json = "[";
        for (uint256 i = 0; i < values.length; i++) {
            if (i > 0) json = string.concat(json, ",");
            json = string.concat(json, VM.toString(values[i]));
        }
        json = string.concat(json, "]");
        gate.mockRaw("attackClaimParentIndices", json);
    }

    // challengerAttacks: list<boolean>
    function mockChallengerAttacks(Solgate.Gate memory gate, bool[] memory values) internal {
        string memory json = "[";
        for (uint256 i = 0; i < values.length; i++) {
            if (i > 0) json = string.concat(json, ",");
            json = string.concat(json, VM.toString(values[i]));
        }
        json = string.concat(json, "]");
        gate.mockRaw("challengerAttacks", json);
    }

}
