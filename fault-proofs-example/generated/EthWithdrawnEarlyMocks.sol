// Auto-generated by solgate from eth_withdrawn_early.gate
// DO NOT EDIT — re-run `solgate codegen` to regenerate
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import {Vm} from "forge-std/Vm.sol";
import {Solgate} from "solgate/Solgate.sol";

library EthWithdrawnEarlyMocks {
    using Solgate for Solgate.Gate;

    Vm constant VM = Vm(address(uint160(uint256(keccak256("hevm cheat code")))));

    // ─── Params ───

    function setMulticall3(Solgate.Gate memory gate, address value) internal {
        gate.setParam("multicall3", value);
    }

    function setDisputeGame(Solgate.Gate memory gate, address value) internal {
        gate.setParam("disputeGame", value);
    }

    // ─── Call Sources ───

    // addressesInTrace: list<address>
    function mockAddressesInTrace(Solgate.Gate memory gate, address[] memory values) internal {
        string memory json = "[";
        for (uint256 i = 0; i < values.length; i++) {
            if (i > 0) json = string.concat(json, ",");
            json = string.concat(json, '"', VM.toString(values[i]), '"');
        }
        json = string.concat(json, "]");
        gate.mockRaw("addressesInTrace", json);
    }

    // delayedWETH: address
    // From: function weth() returns (address)
    function mockDelayedWETH(Solgate.Gate memory gate, address value) internal {
        gate.mockAddress("delayedWETH", value);
    }

    // claims: list<tuple<address>>
    // From: function claimCredit(address _recipient)
    struct ClaimsEntry {
        address field0;
    }

    function mockClaims(Solgate.Gate memory gate, ClaimsEntry[] memory entries) internal {
        string memory json = "[";
        for (uint256 i = 0; i < entries.length; i++) {
            if (i > 0) json = string.concat(json, ",");
            json = string.concat(json, "[",
                '"', VM.toString(entries[i].field0), '"',
            "]");
        }
        json = string.concat(json, "]");
        gate.mockRaw("claims", json);
    }

    // withdrawals: list<tuple<address, integer>>
    // From: function withdraw(address _guy, uint256 _wad)
    struct WithdrawalsEntry {
        address field0;
        int256 field1;
    }

    function mockWithdrawals(Solgate.Gate memory gate, WithdrawalsEntry[] memory entries) internal {
        string memory json = "[";
        for (uint256 i = 0; i < entries.length; i++) {
            if (i > 0) json = string.concat(json, ",");
            json = string.concat(json, "[",
                '"', VM.toString(entries[i].field0), '"', ",",
                VM.toString(entries[i].field1),
            "]");
        }
        json = string.concat(json, "]");
        gate.mockRaw("withdrawals", json);
    }

    // claimsAndWithdrawals: list<tuple<address, integer>>
    struct ClaimsAndWithdrawalsEntry {
        address field0;
        int256 field1;
    }

    function mockClaimsAndWithdrawals(Solgate.Gate memory gate, ClaimsAndWithdrawalsEntry[] memory entries) internal {
        string memory json = "[";
        for (uint256 i = 0; i < entries.length; i++) {
            if (i > 0) json = string.concat(json, ",");
            json = string.concat(json, "[",
                '"', VM.toString(entries[i].field0), '"', ",",
                VM.toString(entries[i].field1),
            "]");
        }
        json = string.concat(json, "]");
        gate.mockRaw("claimsAndWithdrawals", json);
    }

    // delayTime: integer
    // From: function delay() returns (uint256)
    function mockDelayTime(Solgate.Gate memory gate, int256 value) internal {
        gate.mockInt("delayTime", value);
    }

    // unlocks: list<tuple<integer, address, tuple<address, integer>>>
    // From: function unlock(address _guy, uint256 _wad)
    struct UnlocksEntryField2 {
        address field0;
        int256 field1;
    }

    struct UnlocksEntry {
        int256 field0;
        address field1;
        UnlocksEntryField2 field2;
    }

    function mockUnlocks(Solgate.Gate memory gate, UnlocksEntry[] memory entries) internal {
        string memory json = "[";
        for (uint256 i = 0; i < entries.length; i++) {
            if (i > 0) json = string.concat(json, ",");
            json = string.concat(json, "[",
                VM.toString(entries[i].field0), ",",
                '"', VM.toString(entries[i].field1), '"', ",",
                "[",
                    '"', VM.toString(entries[i].field2.field0), '"', ",",
                    VM.toString(entries[i].field2.field1),
                "]",
            "]");
        }
        json = string.concat(json, "]");
        gate.mockRaw("unlocks", json);
    }

    // disputeGameUnlocks: list<tuple<integer, address, integer>>
    struct DisputeGameUnlocksEntry {
        int256 field0;
        address field1;
        int256 field2;
    }

    function mockDisputeGameUnlocks(Solgate.Gate memory gate, DisputeGameUnlocksEntry[] memory entries) internal {
        string memory json = "[";
        for (uint256 i = 0; i < entries.length; i++) {
            if (i > 0) json = string.concat(json, ",");
            json = string.concat(json, "[",
                VM.toString(entries[i].field0), ",",
                '"', VM.toString(entries[i].field1), '"', ",",
                VM.toString(entries[i].field2),
            "]");
        }
        json = string.concat(json, "]");
        gate.mockRaw("disputeGameUnlocks", json);
    }

    // unlockTimestamps: list<integer>
    function mockUnlockTimestamps(Solgate.Gate memory gate, int256[] memory values) internal {
        string memory json = "[";
        for (uint256 i = 0; i < values.length; i++) {
            if (i > 0) json = string.concat(json, ",");
            json = string.concat(json, VM.toString(values[i]));
        }
        json = string.concat(json, "]");
        gate.mockRaw("unlockTimestamps", json);
    }

    // unlocksAndAmounts: [map type — not directly translatable to Solidity]

    // hasUnlockedCredit: list<boolean>
    // From: function hasUnlockedCredit(address) view returns (bool)
    function mockHasUnlockedCredit(Solgate.Gate memory gate, bool[] memory values) internal {
        string memory json = "[";
        for (uint256 i = 0; i < values.length; i++) {
            if (i > 0) json = string.concat(json, ",");
            json = string.concat(json, VM.toString(values[i]));
        }
        json = string.concat(json, "]");
        gate.mockRaw("hasUnlockedCredit", json);
    }

    // currTimestamp: integer
    function mockCurrTimestamp(Solgate.Gate memory gate, int256 value) internal {
        gate.mockInt("currTimestamp", value);
    }

    // invalidWithdrawals: list<boolean>
    function mockInvalidWithdrawals(Solgate.Gate memory gate, bool[] memory values) internal {
        string memory json = "[";
        for (uint256 i = 0; i < values.length; i++) {
            if (i > 0) json = string.concat(json, ",");
            json = string.concat(json, VM.toString(values[i]));
        }
        json = string.concat(json, "]");
        gate.mockRaw("invalidWithdrawals", json);
    }

}
